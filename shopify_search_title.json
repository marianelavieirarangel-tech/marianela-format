{
  "query": "query Search($query: String!) { products(first:20, query: $query) { nodes { id title handle variants(first:10) { nodes { id title sku } } } } }",
  "variables": { "query": "title:\"Bikini\"" }
}
